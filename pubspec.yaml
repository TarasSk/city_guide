name: city_guide_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  theme:
    path: ./packages/theme
  data:
    path: ./packages/data
  domain:
    path: ./packages/domain
  firebase_core: ^3.12.1
  go_router: ^14.8.1
  get_it: ^8.0.3
  flutter_bloc: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  melos: ^6.3.2
flutter:
  uses-material-design: true
